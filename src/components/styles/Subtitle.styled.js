import styled from 'styled-components'

const StyledSubtitle = styled.span`
  font-size: 1rem;
  font-weight: 700;
  color: ${({ theme }) => theme.colors.darkGrayishBlueText};
`

export default StyledSubtitle
